---
layout: default
sidebar: classes
title: "MariaDbStatementBuilder"
description: "A class used to build the SQL statements for the MySql.Data-based MariaDb data provider."
permalink: /class/mariadb/mariadb/mariadbstatementbuilder
tags: [repodb, mariadbstatementbuilder, mariadb]
parent: "MariaDb"
grand_parent: CLASSES
nav_order: 5
---

# MariaDbStatementBuilder

---

This class is the [BaseStatementBuilder](/class/basestatementbuilder)-derived implementation for the `MySql.Data`-based [RepoDb.MariaDb](https://www.nuget.org/packages/RepoDb.MariaDb) package. It is automatically registered by [MariaDbBootstrap](/class/mariadb/mariadb/mariadbbootstrap) — you do not need to instantiate it directly under normal use.

It generates `` `table` ``-quoted, `@`-parameterized SQL, and uses `LIMIT`/`OFFSET` for [BatchQuery](/operation/batchquery) and the internal skip/take query.

For [Insert](/operation/insert) and [InsertAll](/operation/insertall), the newly generated identity value is returned via `LAST_INSERT_ID()`; for a non-identity primary key it is echoed back from the supplied parameter instead.

[Merge](/operation/merge) and [MergeAll](/operation/mergeall) are compiled as `INSERT ... ON DUPLICATE KEY UPDATE ...` (MariaDB's native upsert), returning `COALESCE(<primary key parameter>, LAST_INSERT_ID())` so the result reflects either the supplied key or the identity value generated by the insert branch.

{: .note }
> That `COALESCE(...)` expression is additionally wrapped in a `CAST(... AS UNSIGNED)`/`CAST(... AS SIGNED)`, chosen from the key column's CLR type (unsigned integer types cast to `UNSIGNED`, signed integer types cast to `SIGNED`, defaulting to `UNSIGNED` when there is no key column) — this avoids a type-mismatch error when combining a signed literal with an unsigned `AUTO_INCREMENT` value.

This builder also strips the extra space the base builder emits before the opening parenthesis of `COUNT`, `MAX`, `MIN` and `SUM` calls (e.g. `COUNT (` becomes `COUNT(`).

{: .note }
> No `convertFieldResolver` is supplied to the base constructor, so — like MySQL — a [Field](/class/field) with an explicit `.Type` is not rendered as a `CAST`/`CONVERT` expression for MariaDb.

## Constructors

```csharp
public MariaDbStatementBuilder()
public MariaDbStatementBuilder(IDbSetting dbSetting,
    IResolver<Field, IDbSetting, string> convertFieldResolver = null,
    IResolver<Type, Type> averageableClientTypeResolver = null)
```

The parameterless constructor resolves `dbSetting` via `DbSettingMapper.Get<MariaDbConnection>()`, so it can only be used after [MariaDbBootstrap](/class/mariadb/mariadb/mariadbbootstrap) has run.

## Usability

Use [StatementBuilderMapper](/mapper/statementbuildermapper) to override it with a custom implementation.

```csharp
StatementBuilderMapper.Add(typeof(MariaDbConnection), new MyCustomMariaDbStatementBuilder(dbSetting), true);
```
